/**
 * Dummy class
 */
export class Dummy {
  /**
   * Gets a person.
   * @param  {string} name
   * @returns An object with a name.
   */
  getPerson(name: string): any {
    return { name: name }
  }
}
